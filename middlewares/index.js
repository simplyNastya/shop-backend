const isValidProductId = require('./isValidProductId')
const isValidFavoriteId = require('./isValidFavoriteId')
const authenticate = require('./authenticate')
// const upload = require('./upload')

module.exports = {
    isValidProductId,
    isValidFavoriteId,
    authenticate,
    // upload,
}